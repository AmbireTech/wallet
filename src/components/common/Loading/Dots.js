import './Loading.scss';

const Dots = () => {
    return (
        <span className="loading-dots">
            <span>.</span>
            <span>.</span>
            <span>.</span>
        </span>
    );
};

export default Dots;