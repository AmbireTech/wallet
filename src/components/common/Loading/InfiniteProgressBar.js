import './Loading.scss';

const InfiniteProgressBar = () => {
    return (
        <div className="progress-bar">
            <div className="inner">
            </div>
        </div>
    );
};

export default InfiniteProgressBar;