name: Build & deploy staging for portfolio-v2

on:
  push:
    branches:
      - feature/portfolio-hook-v2
  pull_request:
    branches:
      - feature/portfolio-hook-v2

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Install Node.js
      uses: actions/setup-node@v1
      with:
        node-version: 16.x
    
    - name: Install NPM packages
      run: npm ci
    
    - name: Build project
      run: npm run build
    
    - name: Upload production-ready build files
      uses: actions/upload-artifact@v2
      with:
        name: production-files
        path: ./build
  deploy-portfolio-hook-v2:
    name: Deploy portfolio-hook-v2
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/feature/portfolio-hook-v2'
    steps:
    - name: Download artifact
      uses: actions/download-artifact@v2
      with:
        name: production-files
        path: ./build

    - name: Deploy to gh-pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./build
        destination_dir: ./portfolio-v2
        cname: wallet.ambire.com
